<% content_for :title, "Trending — ##{@community.slug}" %>

<section class="py-8">
  <div class="mx-auto max-w-6xl px-4 grid grid-cols-1 md:grid-cols-2 gap-6">
    <div>
      <h1 class="text-2xl font-bold">Trending in #<%= @community.name %> (last <%= @hours %>h)</h1>
      <ul class="mt-3 space-y-2">
        <% @top_tags.each do |t| %>
          <li class="flex items-center justify-between rounded-lg border border-slate-200 dark:border-slate-800 p-2">
            <%= link_to "##{t.name}", community_tag_path(@community.slug, t.name), class: 'text-sky-600 hover:underline' %>
            <span class="text-slate-500 text-sm"><%= t.attributes['uses_count'] %></span>
          </li>
        <% end %>
      </ul>
    </div>
    <div>
      <h2 class="text-2xl font-bold">Trending Topics</h2>
      <ul class="mt-3 space-y-2">
        <% @top_topics.each do |tp| %>
          <li class="flex items-center justify-between rounded-lg border border-slate-200 dark:border-slate-800 p-2">
            <%= link_to "##{tp.name}", community_topic_path(@community.slug, tp.slug), class: 'text-sky-600 hover:underline' %>
            <span class="text-slate-500 text-sm"><%= tp.attributes['posts_count'] %></span>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</section>

