local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# For production on Render, use S3 or compatible storage.
# amazon:
#   service: S3
#   access_key_id: <%= ENV['AWS_ACCESS_KEY_ID'] %>
#   secret_access_key: <%= ENV['AWS_SECRET_ACCESS_KEY'] %>
#   region: <%= ENV['AWS_REGION'] || 'us-east-1' %>
#   bucket: <%= ENV['AWS_S3_BUCKET'] %>

# Rails 7.1+ built-in database storage (stores files in DB)
database:
  service: Database
